h1 Wait Your Turn
= link_to "Try To Take Turn", go_fish_path(@go_fish.game_id)

p Your Cards:
ul
  // Get the cards of the current player and list them all
  - @go_fish.find_player_with_user_id(current_user.id).hand.each do |card|
    li = card.description
    
