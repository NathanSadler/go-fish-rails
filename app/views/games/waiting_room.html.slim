h1 = @game.title
= link_to "Try To Start Game", @game, method: "patch"
p Waiting for game to start...
p Users in game:
ul
  - GameUser.where(game_id: @game.id).map(&:user_id).each do |user_id|
    li = User.find(user_id).name
